<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mapper.MemberMapper">
	<select id="getList" resultType="kr.co.domain.MemberVO">
		select * from member
	</select>
	<select id="getGroupMember" resultType="kr.co.domain.MemberVO">
		SELECT member.* FROM member JOIN mgroup ON member.m_id = mgroup.m_id WHERE mgroup.g_name = #{g_name}
	</select>
	<insert id="insert">
		insert into member(m_id, m_pw, m_name, m_email, m_public) values(#{m_id}, #{m_pw}, #{m_name}, #{m_email}, #{m_public});
	</insert>
	
</mapper>